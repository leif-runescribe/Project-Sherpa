import React, { useState } from 'react';
// import { motion } from 'framer-motion';
import { Link } from 'react-router-dom';

const ElegantJoinUs = () => {
  const [hovered, setHovered] = useState(false);

  return (
    <div className="relative px-20  min-h-screen flex items-center justify-center overflow-hidden">
     
      
    </div>
  );
};

export default ElegantJoinUs;
